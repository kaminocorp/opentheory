# Project Stewardship & Collaboration — Implementation Plan

> **Companion to:** `docs/executing/project-stewardship-and-collaboration.md` (the proposal —
> read it first for *why*). This doc is the *how*: methodical, checkbox-tracked tasks across
> three small releases.
>
> **Sequence:** `0.8.1` (ownership + self-edit) → `0.8.2` (account `@username`) → `0.8.3`
> (collaborators + invitation inbox). Each release is independently shippable and ends green.

---

## 0. Global conventions & guardrails (apply to every task)

Keep these invariants in view — they're the ones easy to break:

- [ ] **Export every new model** from `backend/app/models/__init__.py` `__all__` — Alembic's
  `env.py` discovers metadata via `from app.models import *`; a model missing from `__all__`
  is silently absent from autogenerated migrations.
- [ ] **Do NOT register** `ProjectMember` / `ProjectInvitation` in `models/append_only.py` —
  they're mutable identity/governance rows (like `Account` and `Branch.status`), not ledger.
- [ ] **Named PG enums:** add to `models/enums.py` as `StrEnum`, map with a stable
  `name=` (`project_role`, `invitation_status`). In **raw-SQL** migration predicates the enum
  **label is the uppercase StrEnum member name** (`'OWNER'`, `'PENDING'`) — lowercase errors.
- [ ] **Declare table constraints on the model AND the migration** (esp. the partial unique
  owner index) so the test harness's `Base.metadata.create_all` builds exactly what Alembic
  installs (the `uq_actors_one_human_per_account` discipline).
- [ ] **Authorization in the service layer:** route handlers stay thin; `ensure_can_manage`
  and invite/accept logic live in services. Unauth → `401`, signed-in non-member → `403`,
  missing → `404`.
- [ ] **One transaction per write flow:** project-create writes Project + owner member +
  contribution and commits **once**; accept-invite writes the member + flips status in one
  commit.
- [ ] **PII posture:** member/invitation public reads use `AccountSummary` (id + display_name +
  username) — **never** email/roles/external_id. `username` is a public handle and is OK to expose.
- [ ] **Register routers** in `backend/app/api/router.py` next to the existing includes.
- [ ] **Update `docs/changelog.md`** at the end of each release (per repo convention).
- [ ] **Migrations are backend-owned**; chain `down_revision` correctly (`0006_accounts` →
  `0007…` → `0008…` → `0009…`).

**Per-release verification gates (must pass before merge):**

```bash
# backend
cd backend && uv run ruff check . && uv run pytest        # DB tests need TEST_DATABASE_URL
# frontend
cd frontend && npm run typecheck && npm run lint && npm run build
```

> **DB tests:** per project policy we don't run destructive pytest against the live DB. Set
> `TEST_DATABASE_URL` to a **throwaway** Postgres only when explicitly greenlit; otherwise the
> DB-backed tests skip (suite still green) and we verify those flows against the live deploy.

---

## Release `0.8.1` — Ownership + self-edit (delivers A, B, D)

**Goal:** every new project has an owner; the owner/admins can edit `title` / `question` /
`description` / `background` / `status`; project creation is finally attributed. Unblocks your
own editing immediately (existing projects get their owner row added by hand in Supabase).

### Backend

- [ ] **T1.1 — Enum + model.**
  - `models/enums.py`: add `class ProjectRole(StrEnum): OWNER="owner"; ADMIN="admin"`.
  - New `models/project_member.py` → `ProjectMember` (`IdMixin`, `TimestampMixin`), table
    `project_members`: `project_id` (FK `projects.id` `CASCADE`, indexed), `account_id` (FK
    `accounts.id` `CASCADE`, indexed), `role` (`Enum(ProjectRole, name="project_role")`),
    `invited_by_account_id` (FK `accounts.id` `SET NULL`, nullable). `__table_args__`:
    `UniqueConstraint("project_id","account_id", name="uq_project_member")` **and** a partial
    unique `Index("uq_project_one_owner","project_id", unique=True, postgresql_where=text("role = 'OWNER'"))`.
  - `models/project.py`: add `background: Mapped[str | None] = mapped_column(Text)`; add
    `members = relationship("ProjectMember", ... cascade="all, delete-orphan")`.
  - `models/__init__.py`: import + add `"ProjectMember"` to `__all__`.
  - **Acceptance:** `uv run python -c "from app.models import ProjectMember"` imports clean.

- [ ] **T1.2 — Schemas.** `schemas/project.py`:
  - `ProjectBase`: add `background: str | None = Field(default=None, max_length=50_000)`.
  - `ProjectCreate`: allow optional `description` + `background` at create (already inherits).
  - New `ProjectUpdate(BaseModel)`: all optional — `title`, `question`, `description`,
    `background`, `status` (no `slug`). Used with `exclude_unset=True`.
  - New `ProjectMemberRead(BaseModel, from_attributes)`: `account: AccountSummary`, `role:
    ProjectRole`, `created_at`. (Embed `AccountSummary` from `schemas/account.py`.)
  - `ProjectRead`/`ProjectOverview`: add `background: str | None`.

- [ ] **T1.3 — Membership service.** New `services/project_members.py`:
  - `async def ensure_can_manage(db, project_id, actor, *, require_owner=False) -> Project`
    — loads the project (`404` if missing), looks up `ProjectMember` by `(project_id,
    actor.account_id)`; raises `403` if absent or (when `require_owner`) not `OWNER`; returns
    the project. Account-less actors (`actor.account_id is None`) → `403`.
  - `list_members(db, project_id) -> list[ProjectMemberRead]` (joins `Account`, ordered owner-first).
  - `remove_member(db, project, account_id, actor)` (owner-only; cannot remove the sole owner).
  - `set_member_role(db, project, account_id, role, actor)` (owner-only; transferring `OWNER`
    demotes the prior owner to `ADMIN` in the same txn to preserve the single-owner index).

- [ ] **T1.3b — `DbSession` import.** Note `api/deps.py` already exports `DbSession` and
  `ActingActor`; reuse them (don't redeclare).

- [ ] **T1.4 — Extract `create_project` into a service (ownership + contribution).**
  - New `services/projects.py::create_project(db, payload, actor)` (the read service file
    already exists — add the write fn there or a sibling): in **one transaction** — add
    `Project`; add `ProjectMember(project, account_id=actor.account_id, role=OWNER,
    invited_by_account_id=actor.account_id)`; add a `Contribution(project_id=project.id,
    actor_id=actor.id, action="create_project", target_type="project", target_id=project.id)`
    (reuse `services/contributions.record_contribution` if its signature fits — it `db.add`s
    without committing, matching the chokepoint pattern); `commit`; `refresh`; return.
  - **Edge:** if `actor.account_id is None` (dev/system actor with no account), skip the owner
    row (can't own without a principal) — or `400`. Decide: for dev-path tests we build an
    account, so require `account_id` and `400` otherwise. Document in the fn.
  - `api/routes/projects.py::create_project`: replace the inline body with
    `await project_service.create_project(db, payload, actor)`.

- [ ] **T1.5 — Update + member routes.** `api/routes/projects.py`:
  - `PATCH /projects/{project_id}` (`ActingActor`): `project = await
    ensure_can_manage(db, project_id, actor)`; apply `payload.model_dump(exclude_unset=True)`
    onto the project; `commit`; return `ProjectRead`. Plain mutation (no checkpoint, no
    append-only).
  - `GET /projects/{project_id}/members` → `list[ProjectMemberRead]` (public, no auth dep).
  - `DELETE /projects/{project_id}/members/{account_id}` (`ActingActor`, owner-only).
  - `PATCH /projects/{project_id}/members/{account_id}` (`ActingActor`, owner-only) — body
    `{ role }`.

- [ ] **T1.6 — Migration `0007_project_stewardship`** (`alembic/versions/`, additive,
  `down_revision="0006_accounts"`):
  - `op.add_column("projects", sa.Column("background", sa.Text(), nullable=True))`.
  - `op.create_table("project_members", …)` with the FKs, `UniqueConstraint`, and the partial
    unique owner index (`op.create_index(..., unique=True, postgresql_where=sa.text("role = 'OWNER'"))`).
    Create the `project_role` enum (let `sa.Enum(..., name="project_role")` create it, or
    `postgresql.ENUM(..., create_type=…)` — match the 0006 idiom).
  - **No data backfill** (owners added by hand in Supabase).
  - `downgrade()`: drop table + enum + column.
  - **Verify:** `uv run alembic upgrade head` then `uv run alembic check` clean on a throwaway DB.

### Frontend

- [ ] **T1.7 — Types.** `types/project.ts`: add `background: string | null` to `Project` +
  `ProjectCreate`; add `ProjectUpdate`. `types/research.ts`: add `background` to
  `ProjectOverview`; add `ProjectRole = "owner" | "admin"` and `ProjectMember = { account:
  AccountSummary; role: ProjectRole; created_at: string }`.

- [ ] **T1.8 — API client.** `lib/api.ts`: `updateProject(id, ProjectUpdate)` (`PATCH`),
  `listProjectMembers(id)`, `removeProjectMember(id, accountId)`, `updateProjectMember(id,
  accountId, role)`. Add a `patchInit(body)` helper alongside `writeInit` (method `"PATCH"`).
  `lib/query-keys.ts`: add `members: (projectId) => ["members", projectId]`.

- [ ] **T1.9 — Capability gate.** In `project-workspace.tsx`, fetch members via
  `useQuery(queryKeys.members(projectId))`; derive `canManageProject = isAuthed &&
  members.some(m => m.account.id === me?.account?.id)` (and `isOwner` for owner-only controls).
  Reuse `useActingIdentity()` for `me`.

- [ ] **T1.10 — Rich-text editor (TipTap → Markdown).** Add deps `@tiptap/react`,
  `@tiptap/starter-kit`, and a markdown serializer (`tiptap-markdown`); add `react-markdown` +
  `remark-gfm` for rendering. New `components/workspace/rich-text-editor.tsx` (value=Markdown
  string, onChange=Markdown) and a `Markdown` render component. Keep it lazy-loaded
  (`next/dynamic`, `ssr:false`) so the editor bundle doesn't bloat the project page.

- [ ] **T1.11 — Edit form.** New `components/workspace/project-edit-form.tsx`: Console-styled
  (`Bay`/`Input`/`Textarea`/`Action`) form for `title` / `question` / `description` / `status`
  + the TipTap editor for `background`; submits `updateProject`; on success invalidates
  `queryKeys.project(id)` + `overview(id)`. Mount an "Edit" `ActionGhost` toggle in the
  workspace header (`project-workspace.tsx`), shown only when `canManageProject` (mirrors the
  "New project" gating).

- [ ] **T1.12 — Background section + Collaborators (read).** On the project page, render a
  collapsible "Background / Context" `Bay` with the Markdown when `project.background`. Add a
  `components/workspace/collaborators-panel.tsx` listing members (`@handle` once `0.8.2` lands;
  display_name until then) + role pill; owner sees remove/role controls. *(Invite UI lands in
  `0.8.3`.)*

### Tests & done

- [ ] **T1.13 — Backend tests** (`tests/test_projects.py` + new `tests/test_project_members.py`):
  create-project now makes an owner row + a `create_project` contribution; `PATCH` authz matrix
  (owner ✓ / admin ✓ / non-member `403` / anon `401` via `dbfree_client`); `background`
  round-trips + nullable; member list shape (no email); single-owner index rejects a second
  owner; owner-only remove/role. Build the acting users with the `internal_funder`-style
  account+actor + `X-Dev-Actor-Id` pattern.
- [ ] **T1.14 — DB-free:** `ProjectUpdate` partial semantics; `PATCH /projects/{id}`
  unauthenticated → `401` before DB (`dbfree_client`).
- [ ] **T1.15 — Verify** (gates above) + **changelog** `0.8.1` entry. Manually add your owner
  row to existing projects in Supabase; confirm edit works on the live deploy.

**Definition of done:** signed-in owner can edit all four fields + status on their projects;
non-members cannot; new projects auto-own + record a creation contribution; suite + frontend
build green.

---

## Release `0.8.2` — Account `@username` (prerequisite for invites)

**Goal:** every account has a unique, auto-generated, renameable public handle, exposed on
`/me` and `AccountSummary`, resolvable for invites.

### Backend

- [ ] **T2.1 — Username generation util.** New `core/usernames.py`:
  `normalize(raw) -> str` (lowercase, strip to `[a-z0-9_]`, collapse, enforce 3–30, pad short),
  `base_from(email, display_name) -> str`, and a small reserved-set constant
  (`{"me","admin","system","accounts","api"}`). Pure functions, unit-tested DB-free.

- [ ] **T2.2 — Model + provisioning.**
  - `models/account.py`: add `username: Mapped[str] = mapped_column(String(30), unique=True,
    nullable=False)`.
  - `api/deps.py::_resolve_or_provision`: when creating the Account, generate a candidate handle
    (`base_from(email, display_name)` → `normalize`), and on the unique-violation retry path,
    append a short suffix and retry (bounded). Generate **inside the existing first-login
    transaction**; the current `IntegrityError`→re-read handles the `external_id` race — extend it
    so a *username* collision regenerates rather than re-reads.
  - **Acceptance:** two provisions from emails with the same local-part get distinct handles.

- [ ] **T2.3 — Schemas.**
  - `schemas/account.py`: add `username: str` to `AccountBase`/`AccountRead`; add `username` to
    `AccountSummary` (public handle — fine to expose). New `AccountUpdate(BaseModel)`:
    `username: str` validated to `^[a-z0-9_]{3,30}$`.
  - `schemas/account.py` or `core`: a `resolve_account_by_identifier(db, identifier)` helper —
    email-shaped (`"@" in identifier`) → match `lower(email)`; else → match `username`
    (lowercased). Returns the account or `None`. (Used by invites in `0.8.3`.)

- [ ] **T2.4 — `PATCH /me`.** New route in `api/routes/me.py` (`ActingActor`): update the
  caller's **own** account `username`; `409` on unique collision (catch `IntegrityError`),
  `422` on pattern/reserved. Returns `MeRead`.

- [ ] **T2.5 — Migration `0008_account_username`** (`down_revision="0007_project_stewardship"`,
  hand-authored backfill):
  1. `op.add_column("accounts", sa.Column("username", sa.String(30), nullable=True))`.
  2. **Backfill** every existing account with a unique handle. Recommended: do it imperatively
     in the migration via `conn = op.get_bind()` — read `(id, email, display_name)`, compute a
     normalized base, track a seen-set, suffix on collision, `UPDATE` each row. (Clearer than
     gnarly SQL for the slug+dedupe logic; few rows today.)
  3. `op.create_unique_constraint("uq_accounts_username", "accounts", ["username"])` and
     `op.alter_column("accounts","username", nullable=False)`.
  - **Ship with T2.2** — old provisioning code can't populate `username`, so the `NOT NULL` would
    break the next sign-in if code lags the migration.
  - `downgrade()`: drop constraint + column.

### Frontend

- [ ] **T2.6 — Types + client.** Add `username` to `Account` / `Me` / `AccountSummary`
  (`types/research.ts`). `lib/api.ts`: `updateMe({ username })` (`PATCH /me`).
- [ ] **T2.7 — Identity surface.** `auth-menu.tsx`: show `@username` under the display name; add a
  small "edit username" field (inline, `updateMe`) with a `409` collision message. Invalidate
  `queryKeys.me` on success. Show `@handle` in the collaborators member list.

### Tests & done

- [ ] **T2.8 — Tests:** DB-free username `normalize`/reserved/pattern; DB-backed
  auto-generation + uniqueness on provision, collision suffixing, `PATCH /me` rename happy-path
  + `409` collision + `422` invalid; `AccountSummary` includes `username`, still no email.
- [ ] **T2.9 — Verify** + **changelog** `0.8.2`. Confirm on the live deploy your existing
  account received a handle and rename works.

**Definition of done:** every account has a unique handle; users can rename; handles show in the
UI; `resolve_account_by_identifier` works for username and email.

---

## Release `0.8.3` — Collaborators + invitation inbox (delivers C)

**Goal:** owner/admin invites an existing user by `@username` or email; the invitee sees it in a
top-right bell inbox and accepts (→ admin) or declines.

### Backend

- [ ] **T3.1 — Enum + model.**
  - `models/enums.py`: `class InvitationStatus(StrEnum): PENDING; ACCEPTED; DECLINED; REVOKED`.
  - New `models/project_invitation.py` → `ProjectInvitation` (`IdMixin`, `TimestampMixin`),
    table `project_invitations`: `project_id` (FK `CASCADE`, indexed), `invitee_account_id` (FK
    `accounts.id` `CASCADE`, indexed), `role` (`project_role`, default `ADMIN`), `status`
    (`invitation_status`, default `PENDING`), `invited_by_account_id` (FK `SET NULL`, nullable),
    `UniqueConstraint("project_id","invitee_account_id", name="uq_project_invitation")`.
  - Export from `models/__init__.py`.

- [ ] **T3.2 — Schemas.** `schemas/` new `invitation.py`: `InvitationCreate { identifier: str;
  role: ProjectRole = ADMIN }`; `InvitationRead { id, project_id, project_title, role, status,
  invited_by: AccountSummary | None, created_at }` (+ `invitee: AccountSummary` for the
  project-side list). PII-safe summaries only.

- [ ] **T3.3 — Service.** New `services/invitations.py`:
  - `invite(db, project, identifier, role, actor)` — owner/admin (`ensure_can_manage`); resolve
    via `resolve_account_by_identifier` (`404` if none); reject self-invite + already-member
    (`409`); upsert the `(project, invitee)` row to `PENDING` (re-invite after decline resets);
    commit; return `InvitationRead`.
  - `my_pending(db, actor)` — caller's `PENDING` invitations (join project for title).
  - `accept(db, invitation_id, actor)` — invitee-only (`403` otherwise); one txn: create
    `ProjectMember(role=invitation.role, invited_by=invitation.invited_by_account_id)` (idempotent
    on `uq_project_member`), set status `ACCEPTED`; commit.
  - `decline(db, invitation_id, actor)` — invitee-only → `DECLINED`.
  - `revoke(db, project, invitation_id, actor)` — owner/admin → `REVOKED`.
  - `list_for_project(db, project, actor)` — owner/admin → pending invites.

- [ ] **T3.4 — Routes.** New `api/routes/invitations.py` (+ register in `api/router.py`):
  - `POST /projects/{id}/invitations`, `GET /projects/{id}/invitations`,
    `DELETE /projects/{id}/invitations/{inv_id}` (all owner/admin).
  - `GET /me/invitations`, `POST /invitations/{inv_id}/accept`,
    `POST /invitations/{inv_id}/decline` (invitee).

- [ ] **T3.5 — Migration `0009_project_invitations`** (`down_revision="0008_account_username"`,
  additive): create `invitation_status` enum + `project_invitations` table (FKs + unique). No
  backfill. `downgrade()` drops both.

### Frontend

- [ ] **T3.6 — Types + client.** `InvitationStatus`, `ProjectInvitation` types; `lib/api.ts`:
  `inviteProjectMember(id, {identifier, role})`, `listProjectInvitations(id)`,
  `revokeInvitation(id, invId)`, `getMyInvitations()`, `acceptInvitation(invId)`,
  `declineInvitation(invId)`. `query-keys.ts`: `myInvitations: ["me","invitations"]`,
  `projectInvitations: (id) => ["invitations", id]`.

- [ ] **T3.7 — Invitation inbox (bell).** New `components/shell/invitation-inbox.tsx`: a bell
  button + badge (count from `getMyInvitations`, `enabled: isAuthed`), dropdown listing each
  pending invite (project title, inviter `@handle`, role) with **Accept**/**Decline**. On accept:
  invalidate `myInvitations` + `members(projectId)` + `projects`; optional toast/route. Mount in
  `app-shell.tsx`'s `ShellHeader` right slot, **before** `<AuthMenu/>`, rendered only when signed in.

- [ ] **T3.8 — Invite form.** Extend `collaborators-panel.tsx`: owner/admin gets an
  invite-by-`@username`-or-email `Input` + role (fixed `admin` for now) + submit
  (`inviteProjectMember`); show this project's pending invitations with a revoke control;
  surface `404 user not found` / `409 already invited` inline.

### Tests & done

- [ ] **T3.9 — Tests** (`tests/test_invitations.py`): invite by username **and** by email →
  `PENDING`; unknown identifier → `404`; self/duplicate → `409`; `GET /me/invitations` returns
  only the caller's pending; accept creates the admin membership (and that admin can then
  `PATCH` the project) + status `ACCEPTED`; decline → no membership; re-invite after decline
  resets to `PENDING`; revoke; non-invitee accept → `403`. Reads omit email.
- [ ] **T3.10 — Verify** + **changelog** `0.8.3`. Live check: invite a second test account, see
  the bell, accept, confirm it can edit.

**Definition of done:** owner/admin invites an existing user by handle or email; invitee
accepts/declines from the bell inbox; accepted admins can edit the project; all gates green.

---

## Appendix — sequencing & risk notes

- **Phase independence:** `0.8.1` ships without usernames (member list shows `display_name`);
  `0.8.2` adds handles; `0.8.3` needs both (`resolve_account_by_identifier` + `ProjectMember`).
  Don't start `0.8.3` backend until `0.8.2` is merged.
- **Migration chain:** `0006 → 0007 → 0008 → 0009`. Each backend code change ships **with** its
  migration (esp. `0008`'s `NOT NULL` username + the provisioning change).
- **Pre-existing nit (out of scope, note it):** the frontend `ProjectStatus` type includes
  `"completed"`, which the backend `ProjectStatus` enum does **not** have. The `PATCH` status
  field validates against the backend enum; if we want `completed`/an explicit "active" lifecycle
  in the edit form, that's a tiny separate enum change — flag, don't silently diverge.
- **Live-DB safety:** backfills (`0007` none, `0008` username) are additive and idempotent;
  still rehearse `upgrade`/`downgrade` on a throwaway DB before the live `alembic upgrade head`.
- **TipTap bundle:** lazy-load the editor; the read path uses only `react-markdown` so public
  viewers don't pay for the editor.
